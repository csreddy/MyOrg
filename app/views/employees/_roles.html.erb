<% if @employee.roles.empty? %>
	<span class="label label-info"> <%= "not assigned" %></span>
	<% else %>
<% @roles = @employee.roles %>
<% @roles.each do |role| %>
<dd><%= link_to role.title, role_path(role) %><br /></dd>
<% end %>
<% end %>
		
	   